import Services from "@/components/Services";
import { SharedLayout } from "@/components/SharedComponent";

const page = () => {
  return (
    <SharedLayout>
      <Services />
    </SharedLayout>
  );
};

export default page;
